---
// Static form markup only; no backend. Accessible labels and semantics.
---

<section class="border border-slate-200 rounded-lg p-6 bg-white">
  <h2 class="font-heading text-navy text-xl font-semibold mb-4">Send us a message</h2>
  <form action="#" method="post" class="space-y-4">
    <div>
      <label for="contact-name" class="block text-sm font-medium text-navy mb-1">Name</label>
      <input
        type="text"
        id="contact-name"
        name="name"
        required
        class="w-full rounded border border-slate-300 px-3 py-2 text-navy focus:border-gold focus:outline-none focus:ring-1 focus:ring-gold"
      />
    </div>
    <div>
      <label for="contact-email" class="block text-sm font-medium text-navy mb-1">Email</label>
      <input
        type="email"
        id="contact-email"
        name="email"
        required
        class="w-full rounded border border-slate-300 px-3 py-2 text-navy focus:border-gold focus:outline-none focus:ring-1 focus:ring-gold"
      />
    </div>
    <div>
      <label for="contact-message" class="block text-sm font-medium text-navy mb-1">Message</label>
      <textarea
        id="contact-message"
        name="message"
        rows={5}
        required
        class="w-full rounded border border-slate-300 px-3 py-2 text-navy focus:border-gold focus:outline-none focus:ring-1 focus:ring-gold"
      />
    </div>
    <button
      type="submit"
      class="inline-flex items-center justify-center font-body font-medium rounded px-4 py-2 bg-gold text-navy hover:bg-gold/90 transition-colors"
      disabled
      aria-describedby="form-note"
    >
      Send message
    </button>
    <p id="form-note" class="text-sm text-slate-500">Form submission is not yet connected. Please use the phone or address below to get in touch.</p>
  </form>
</section>
