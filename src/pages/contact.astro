---
import BaseLayout from '@/components/layout/BaseLayout.astro';
import ContactForm from '@/components/sections/ContactForm.astro';
import Icon from '@/components/ui/Icon.astro';
import { MapPin, Phone, Mail } from '@/icons/lucide-icons';
import { siteMetadata } from '@/data/siteMetadata';
---

<BaseLayout title="Contact — J Thomas Financial" description="Get in touch with J Thomas Financial.">
  <main class="max-w-4xl mx-auto px-4 py-12">
    <h1 class="font-heading text-navy text-3xl font-semibold mb-8">Contact Us</h1>
    <div class="grid gap-8 md:grid-cols-2">
      <div class="space-y-6">
        <div class="flex gap-3">
          <Icon icon={MapPin} size={24} class="text-gold shrink-0" />
          <div>
            <p class="font-medium text-navy">Address</p>
            <p class="text-neutral-600">{siteMetadata.contact.address}</p>
          </div>
        </div>
        <div class="flex gap-3">
          <Icon icon={Phone} size={24} class="text-gold shrink-0" />
          <div>
            <p class="font-medium text-navy">Phone</p>
            <a href={`tel:${siteMetadata.contact.phone.replace(/\D/g, '')}`} class="text-neutral-600 hover:text-gold">{siteMetadata.contact.phone}</a>
          </div>
        </div>
        <div class="flex gap-3">
          <Icon icon={Mail} size={24} class="text-gold shrink-0" />
          <div>
            <p class="font-medium text-navy">Email</p>
            <a href={`mailto:${siteMetadata.contact.email}`} class="text-neutral-600 hover:text-gold">{siteMetadata.contact.email}</a>
          </div>
        </div>
        <div class="pt-2">
          <p class="font-medium text-navy mb-2">Location</p>
          <div class="rounded-lg overflow-hidden border border-slate-200 aspect-video w-full max-w-sm">
            <iframe
              src="https://www.google.com/maps?q=21851+Center+Ridge+Rd,+Rocky+River,+OH+44116&output=embed"
              width="100%"
              height="100%"
              style="border:0;"
              allowfullscreen=""
              loading="lazy"
              referrerpolicy="no-referrer-when-downgrade"
              title="J Thomas Financial office location"
            />
          </div>
          <p class="text-neutral-600 text-sm mt-1">21851 Center Ridge Rd, Rocky River, OH 44116</p>
          <a
            href="https://www.google.com/maps/dir/?api=1&destination=21851+Center+Ridge+Rd,+Rocky+River,+OH+44116"
            target="_blank"
            rel="noopener noreferrer"
            class="text-gold hover:underline text-sm mt-1 inline-block"
            aria-label="Get directions to office"
          >
            Get directions →
          </a>
        </div>
      </div>
      <ContactForm />
    </div>
  </main>
</BaseLayout>
