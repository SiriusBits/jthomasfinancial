---
import BaseLayout from '@/components/layout/BaseLayout.astro';
import Card from '@/components/ui/Card.astro';
import Button from '@/components/ui/Button.astro';
import { ChevronRight } from '@/icons/lucide-icons';
import { servicesList } from '@/data/services';
---

<BaseLayout title="Services — J Thomas Financial" description="Tax planning, payroll, financial statements, and consulting.">
  <main class="max-w-6xl mx-auto px-4 py-12">
    <h1 class="font-heading text-navy text-3xl font-semibold mb-2">Our Services</h1>
    <p class="text-neutral-600 mb-10 max-w-2xl">We offer a range of professional finance and accounting services to support your business.</p>
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {servicesList.map((service) => (
        <Card title={service.title}>
          <p class="text-neutral-600 text-sm mb-4">{service.shortDescription}</p>
          <Button href={`/services/${service.slug}`} variant="secondary" icon={ChevronRight} iconPosition="right">
            Learn more
          </Button>
        </Card>
      ))}
    </div>
  </main>
</BaseLayout>
