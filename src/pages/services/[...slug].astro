---
export function getStaticPaths() {
  const slugs = ['tax-planning', 'payroll', 'financial-statements', 'consulting'];
  return slugs.map((slug) => ({ params: { slug } }));
}

const { slug } = Astro.params;
return Astro.redirect(`/services#${slug}`);
---
